<% if @user %>
 <%if I18n.locale=='ru'%> 
	<%= @user.email %>, поменять пороль можно по следующей ссылке:

	<%= @url %>

 <%elsif I18n.locale=='fr'%> 	
	<%= @user.email %>, pour modifier votre mot de passe suivez le lien ci-dessous:
	
	<%= @url %>
	
 <%else%>	
	<%= @user.email %>, you can change your password at this URL:

	<%= @url %>

 <%end%>

<% end %>